name: rag-study-assistant

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - pytest
  - pip:
      - pymupdf
      - rank-bm25
      - pydantic
      - python-dotenv
      - streamlit
      - plotly
      - requests
      - openai
